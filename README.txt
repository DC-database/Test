# QuickBites — Menu + Admin (Dark Mode, QAR, Firebase Ready)
- `index.html` (menu), `admin.html` (admin), `styles.css`, `app.js`, `admin.js`
- `firebase-config.sample.js` → fill and rename to `firebase-config.js`
- `seed/menu.json` → import under `menu/` in Realtime Database
Switch to Firebase by editing `app.js`: `const DATA_MODE='firebase'`.
